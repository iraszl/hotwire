= form_with(model: tweet, id: dom_id(tweet)) do |form|
  - if tweet.errors.any?
    .alert.alert-warning
      - tweet.errors.each do |error|
        = error.full_message
  .form-group
    = form.text_area :body, class: "form-control bg-light", rows: 3
  .form-group.float-left
    = form.submit 'Send Tweet', class: "btn btn-primary"
